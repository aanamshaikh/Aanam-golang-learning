using same method for all options


implement a struct 
set values for each value = default values 
just for printing the name 
then tree cmd = info.name()
-p then = info.permission
-f then =path as relative path

method signature: 
(tree  skipdir subcmd level dirName) 

can use switch case stmt


set the value for the options in struct either the default or passed by user
pass it to the main func


1. tree src done



2. tree -d src done
3. tree -f src done



4. tree -p src done
5 . level
 print the statement as per the level

  








rootCmd, dir,perm,level,number ,dirName

-n =filename
-f = relPath
-X= 